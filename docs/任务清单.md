# 系统改进任务清单

## 第一阶段：先稳住主链路

- [x] 1. 复盘画像查询安全降级，避免买点页/三池页因 DB 异常整体失败
- [x] 2. 路由层收口，抽出 `decision` / `stock` 编排服务，减少路由直接拼装业务
- [x] 3. 统一快照写入入口，明确哪些页面允许“写入失败但响应成功”
- [x] 4. 修复当前失败测试，恢复主干为全绿

## 第二阶段：治理数据与配置

- [x] 5. 引入 Alembic 迁移，移除启动阶段的 `ALTER TABLE`
- [x] 6. 收紧配置默认值：`debug`、`secret_key`、数据库地址、Webhook、LLM key
- [x] 7. 整理 `.env.example`、README、Docker 配置，消除文档与实现偏差

## 第三阶段：收敛边界与重复逻辑

- [x] 8. 抽取统一的持仓读取/补价/账户构建逻辑，消除 `account.py` 与 `decision_context.py` 重复
- [x] 9. 抽取统一的数据网关，隔离 Tushare/DB/快照访问
- [x] 10. 统一 Web 与小程序 API 配置，移除小程序硬编码 `localhost`

## 第四阶段：提升可维护性

- [x] 11. 将规则阈值参数化，支持策略版本与回放
- [x] 12. 增加 API 集成测试，覆盖应用启动与关键业务路由
- [x] 13. 增加关键页面联调测试，覆盖前端页面的主链路请求与展示
- [x] 14. 增加任务调度的幂等、重试和运行状态可观测性
